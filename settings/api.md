# API

`/dashboard/settings/api`

## Enable API user

Toggles the access to the API for authenticated users.

## Enable API guest

Toggles the access to the API for guest users (not authenticated). We recommend to keep this disabled unless you need it.

## Public API key

The public API key is intended to be used by guest users to access public resources through the API.
